[package]
name = "stutter-common"
version = "0.1.0"
edition.workspace = true
description = "Shared user-space and eBPF data types for stutter."
license.workspace = true
repository.workspace = true
readme.workspace = true
rust-version.workspace = true
publish = false

[features]
default = []
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }

[lib]
path = "src/lib.rs"
