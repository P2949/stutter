# stutter Artifact Examples

These directories contain minimal, sanitized run artifacts for the current
artifact schema version. They are small contract examples for tests,
documentation, benchmarking harnesses, and offline automation.

They are not benchmark results. Process names, timestamps, hardware metadata,
and paths are fake or empty on purpose.
