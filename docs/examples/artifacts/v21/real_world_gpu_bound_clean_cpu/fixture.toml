name = "real_world_gpu_bound_clean_cpu"
schema_version = 21
source = "sanitized-real-recording"
quality_expectation = "High"
description = "GPU busy was high during a frame spike while CPU pressure stayed clean."

[expected]
primary_cause = "GpuBoundCandidate"
accepted_confidence = [
    "Low",
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 3
irq_events = 0
gpu_samples = 1
frames = 4
block_io_events = 0

[expected.evidence]
contains = ["GPU busy"]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
