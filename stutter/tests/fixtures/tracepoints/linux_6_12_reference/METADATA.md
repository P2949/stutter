# linux_6_12_reference tracepoint fixtures

Reference tracepoint formats for a Linux 6.12-family tracefs events tree.

These files are used to validate stutter's parser and expected tracepoint
offsets against a known-compatible runtime format. They are fixtures only; the
running kernel is still validated at runtime by preflight.
