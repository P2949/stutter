name = "real_irq_overlap"
schema_version = 21
source = "sanitized-real-recording"
quality_expectation = "High"
description = "IRQ handler overlapped the scheduler-latency cluster strongly enough to be the primary candidate."

[expected]
primary_cause = "IrqDelayCandidate"
accepted_confidence = [
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 3
irq_events = 1
gpu_samples = 0
frames = 0
block_io_events = 0
foreground_events = 0

[expected.evidence]
contains = ["IRQ"]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
