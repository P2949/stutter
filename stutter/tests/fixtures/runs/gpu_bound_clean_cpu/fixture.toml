name = "gpu_bound_clean_cpu"
schema_version = 22
source = "synthetic-contract"
quality_expectation = "High"
description = "Synthetic GPU-bound fixture with high GPU busy and clean CPU pressure."

[expected]
primary_cause = "GpuBoundCandidate"
accepted_confidence = [
    "Low",
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 1
irq_events = 0
gpu_samples = 1
frames = 3
block_io_events = 0
foreground_events = 0

[expected.evidence]
contains = ["GPU busy"]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
