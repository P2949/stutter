name = "cpu_pressure"
schema_version = 21
source = "synthetic-contract"
quality_expectation = "High"
description = "Synthetic CPU pressure fixture with high CPU PSI near a scheduler-latency spike."

[expected]
primary_cause = "CpuPressureCandidate"
accepted_confidence = [
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 1
irq_events = 0
gpu_samples = 0
frames = 0
block_io_events = 0

[expected.evidence]
contains = ["high CPU PSI"]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
