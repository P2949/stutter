name = "real_world_block_io_stall"
schema_version = 21
source = "sanitized-real-recording"
quality_expectation = "High"
description = "Block I/O request overlapped the scheduler-latency cluster strongly enough to be the primary candidate."

[expected]
primary_cause = "BlockIoCandidate"
accepted_confidence = [
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 3
irq_events = 0
gpu_samples = 0
frames = 0
block_io_events = 1

[expected.evidence]
contains = ["block I/O"]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
