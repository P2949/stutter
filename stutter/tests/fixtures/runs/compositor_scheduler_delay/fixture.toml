name = "compositor_scheduler_delay"
schema_version = 21
source = "synthetic-edge-case"
quality_expectation = "High"
description = "Synthetic edge-case fixture for compositor thread scheduler delay during a visible frame spike."

[expected]
primary_cause = "CompositorSchedulerDelay"
accepted_confidence = [
    "Medium",
    "High",
]
data_quality = "High"

[expected.artifacts]
spikes = 3
intervals = 3
irq_events = 0
gpu_samples = 1
frames = 4
block_io_events = 0
foreground_events = 0

[expected.evidence]
contains = [
    "compositor thread",
    "delayed",
]

[privacy]
titles_redacted = true
paths_redacted = true
hostnames_redacted = true
usernames_redacted = true
