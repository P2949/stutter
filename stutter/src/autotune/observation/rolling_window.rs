#![allow(unused_imports)] // Transitional rolling-window facade while old paths are preserved.
// Exit: remove this marker once the described migration is complete and the local allow is no longer needed.

pub(crate) use crate::autotune::rolling_window::RollingWindow;
