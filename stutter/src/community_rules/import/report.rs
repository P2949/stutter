#![allow(unused_imports)] // Transitional report home for community-rule import results.
// Exit: remove this marker once the described migration is complete and the local allow is no longer needed.

pub(crate) use crate::community_rules::importer::ImportReport;
